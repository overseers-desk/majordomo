# Security configuration for CGI deployment
# When deploying to Apache with files in document root, this protects sensitive files
# Non-Apache deployments (WSGI, Gunicorn, Docker) will ignore this file

# Deny access to Python source files
<FilesMatch "\.(py)$">
    Require all denied
</FilesMatch>

# Protect config directory and all its contents
# Protection is enforced by a separate .htaccess file inside config/

