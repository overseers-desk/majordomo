# majordomo — notes for AI sessions

Read Google Chat and report task assignments across spaces and dates. `DESIGN.md` holds the architecture, `PLAN.md` the delivery shape, `docs/RELEASING.md` the release steps.

The design rules that must hold are in [`INVARIANTS.md`](INVARIANTS.md); a change that breaks one is a design change, the owner's to make.
